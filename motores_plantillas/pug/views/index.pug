h2 Tabla de autos
if length 
    table 
        tr 
            th Marca y modelo
            th Precio
            th Imagen
        each val in productos 
            tr 
                td= val.titulo 
                td= val.precio 
                td 
                    img(src=val.imagen, border="3", height="150", width="300")
else
    h3 No se han encontrado productos 
form(action="", method="post") 
    button(type="submit") Volver al inicio